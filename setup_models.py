from utils.download_models import download_models


def download_all_models():
    """
    This function will download all pre-trained models for TTS
    :return:
    """
    download_models()


if __name__ == "__main__":
    download_all_models()
